spec:
  image: ruby:2.4
  tags:
    - docker
  script:
    - bundle install --path vendor --without production --jobs $(nproc) > /dev/null
    - gem build pry-try.gemspec
    - gem install *.gem
    - echo 'puts "Hello AS " + ActiveSupport.version.to_s;exit' | pry-try activesupport